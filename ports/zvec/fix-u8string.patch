diff --git a/src/ailego/utility/file_helper.cc b/src/ailego/utility/file_helper.cc
index 5026f40..15cc6f2 100644
--- a/src/ailego/utility/file_helper.cc
+++ b/src/ailego/utility/file_helper.cc
@@ -79,7 +79,8 @@ fs::path FileHelper::PathFromUtf8(const std::string &s) {
 }
 
 std::string FileHelper::PathToUtf8(const fs::path &p) {
-  return p.u8string();
+  auto _u8s = p.u8string();
+  return std::string(_u8s.begin(), _u8s.end());
 }
 
 #if defined(_WIN32) || defined(_WIN64)
diff --git a/src/db/common/file_helper.cc b/src/db/common/file_helper.cc
index 3302e9a..2cf315a 100644
--- a/src/db/common/file_helper.cc
+++ b/src/db/common/file_helper.cc
@@ -65,7 +65,8 @@ void FileHelper::CleanupDirectory(const std::string &backup_dir,
   std::error_code ec;
   for (const auto &entry : std::filesystem::directory_iterator(
            ailego::FileHelper::PathFromUtf8(backup_dir), ec)) {
-    std::string name = entry.path().filename().u8string();
+    auto _u8nm = entry.path().filename().u8string();
+    std::string name(_u8nm.begin(), _u8nm.end());
     if (name.compare(0, prefix_len, prefix_name) == 0) {
       candidates.emplace_back(name);
     }
diff --git a/src/db/index/common/version_manager.cc b/src/db/index/common/version_manager.cc
index 1ca095d..693a7b3 100644
--- a/src/db/index/common/version_manager.cc
+++ b/src/db/index/common/version_manager.cc
@@ -212,12 +212,14 @@ Result<VersionManager::Ptr> VersionManager::Recovery(const std::string &path) {
 
   for (const auto &entry : fs::directory_iterator(u8path)) {
     if (entry.is_regular_file()) {
-      std::string filename = entry.path().filename().u8string();
+      auto _u8fn = entry.path().filename().u8string();
+      std::string filename(_u8fn.begin(), _u8fn.end());
       if (std::regex_match(filename, match, regex)) {
         uint64_t id = std::stoull(match[1].str());
         if (id > max_id || max_id == UINT64_MAX) {
           max_id = id;
-          version_path = entry.path().u8string();
+          auto _u8vp = entry.path().u8string();
+          version_path = std::string(_u8vp.begin(), _u8vp.end());
         }
       }
     }
diff --git a/src/db/index/storage/store_helper.h b/src/db/index/storage/store_helper.h
index abb4599..b18b07e 100644
--- a/src/db/index/storage/store_helper.h
+++ b/src/db/index/storage/store_helper.h
@@ -41,7 +41,7 @@ namespace zvec {
 
 inline FileFormat InferFileFormat(const std::string &file_path) {
   std::string ext =
-      ailego::FileHelper::PathFromUtf8(file_path).extension().u8string();
+      ailego::FileHelper::PathFromUtf8(file_path).extension().string();
   std::transform(ext.begin(), ext.end(), ext.begin(), ::tolower);
   if (ext == ".parquet") {
     return FileFormat::PARQUET;
