diff --git a/src/server/security_middleware.cpp b/src/server/security_middleware.cpp
index b079443..d53e25d 100644
--- a/src/server/security_middleware.cpp
+++ b/src/server/security_middleware.cpp
@@ -82,7 +82,7 @@ BeforeHook RateLimitMiddleware::create_hook()
         if (stats.timestamps.size() >= max_requests_)
         {
             // Return rate limit error
-            return Json{
+            return std::optional<Json>(Json{
                 {"error",
                  Json{{"code", -32000}, // JSON-RPC server error
                       {"message", "Rate limit exceeded for route: " + route},
@@ -91,7 +91,7 @@ BeforeHook RateLimitMiddleware::create_hook()
                             {"limit", max_requests_},
                             {"window_seconds",
                              std::chrono::duration_cast<std::chrono::seconds>(window_).count()},
-                            {"current_count", stats.timestamps.size()}}}}}};
+                            {"current_count", stats.timestamps.size()}}}}}});
         }
 
         // Record this request
@@ -133,11 +133,11 @@ BeforeHook ConcurrencyLimitMiddleware::create_before_hook()
             current_count_.fetch_sub(1);
 
             // Return concurrency limit error
-            return Json{{"error", Json{{"code", -32000}, // JSON-RPC server error
+            return std::optional<Json>(Json{{"error", Json{{"code", -32000}, // JSON-RPC server error
                                        {"message", "Concurrency limit exceeded"},
                                        {"data", Json{{"route", route},
                                                      {"limit", max_concurrent_},
-                                                     {"current", current}}}}}};
+                                                     {"current", current}}}}}});
         }
 
         return std::nullopt; // Continue to normal handler
