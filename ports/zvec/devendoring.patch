diff --git a/CMakeLists.txt b/CMakeLists.txt
index ad9df29..f4e1878 100644
--- a/CMakeLists.txt
+++ b/CMakeLists.txt
@@ -99,9 +99,74 @@ if(DEFINED ENV{USE_OSS_MIRROR} AND NOT "$ENV{USE_OSS_MIRROR}" STREQUAL "")
 endif()
 message(STATUS "USE_OSS_MIRROR:${USE_OSS_MIRROR}")
 
-cc_directory(thirdparty)
+find_package(Arrow CONFIG REQUIRED)
+find_package(Parquet CONFIG REQUIRED)
+find_package(ArrowCompute CONFIG REQUIRED)
+find_package(ArrowAcero CONFIG REQUIRED)
+find_package(ArrowDataset CONFIG REQUIRED)
+find_package(glog CONFIG REQUIRED)
+find_package(gflags CONFIG REQUIRED)
+find_package(roaring CONFIG REQUIRED)
+find_package(RocksDB CONFIG REQUIRED)
+find_package(Protobuf CONFIG REQUIRED)
+find_package(antlr4-runtime CONFIG REQUIRED)
+
+if(TARGET gflags::gflags AND NOT TARGET gflags)
+    add_library(gflags ALIAS gflags::gflags)
+elseif(TARGET gflags::gflags_static AND NOT TARGET gflags)
+    add_library(gflags ALIAS gflags::gflags_static)
+elseif(NOT TARGET gflags)
+    message(FATAL_ERROR "Neither gflags::gflags nor gflags::gflags_static was found")
+endif()
+
+if(TARGET glog::glog AND NOT TARGET glog)
+    add_library(glog ALIAS glog::glog)
+endif()
+if(TARGET roaring::roaring AND NOT TARGET roaring)
+    add_library(roaring ALIAS roaring::roaring)
+endif()
+if(TARGET RocksDB::rocksdb AND NOT TARGET rocksdb)
+    add_library(rocksdb ALIAS RocksDB::rocksdb)
+endif()
+
+if(TARGET Parquet::parquet_static AND NOT TARGET Arrow::parquet_static)
+    add_library(Arrow::parquet_static INTERFACE IMPORTED)
+    target_link_libraries(Arrow::parquet_static INTERFACE Parquet::parquet_static)
+endif()
+if(TARGET ArrowCompute::arrow_compute_static AND NOT TARGET Arrow::arrow_compute)
+    add_library(Arrow::arrow_compute INTERFACE IMPORTED)
+    target_link_libraries(Arrow::arrow_compute INTERFACE ArrowCompute::arrow_compute_static)
+elseif(TARGET ArrowCompute::arrow_compute_shared AND NOT TARGET Arrow::arrow_compute)
+    add_library(Arrow::arrow_compute INTERFACE IMPORTED)
+    target_link_libraries(Arrow::arrow_compute INTERFACE ArrowCompute::arrow_compute_shared)
+endif()
+if(TARGET ArrowAcero::arrow_acero_static AND NOT TARGET Arrow::arrow_acero)
+    add_library(Arrow::arrow_acero INTERFACE IMPORTED)
+    target_link_libraries(Arrow::arrow_acero INTERFACE ArrowAcero::arrow_acero_static)
+elseif(TARGET ArrowAcero::arrow_acero_shared AND NOT TARGET Arrow::arrow_acero)
+    add_library(Arrow::arrow_acero INTERFACE IMPORTED)
+    target_link_libraries(Arrow::arrow_acero INTERFACE ArrowAcero::arrow_acero_shared)
+endif()
+if(TARGET ArrowDataset::arrow_dataset_static AND NOT TARGET Arrow::arrow_dataset)
+    add_library(Arrow::arrow_dataset INTERFACE IMPORTED)
+    target_link_libraries(Arrow::arrow_dataset INTERFACE ArrowDataset::arrow_dataset_static)
+elseif(TARGET ArrowDataset::arrow_dataset_shared AND NOT TARGET Arrow::arrow_dataset)
+    add_library(Arrow::arrow_dataset INTERFACE IMPORTED)
+    target_link_libraries(Arrow::arrow_dataset INTERFACE ArrowDataset::arrow_dataset_shared)
+endif()
+
+if(NOT TARGET sparsehash)
+    add_library(sparsehash INTERFACE)
+endif()
+if(NOT TARGET magic_enum)
+    add_library(magic_enum INTERFACE)
+endif()
+
+if(TARGET antlr4_static AND NOT TARGET antlr4)
+    add_library(antlr4 ALIAS antlr4_static)
+endif()
+include_directories(${PROJECT_ROOT_DIR}/thirdparty/RaBitQ-Library/RaBitQ-Library-0.1/include)
 cc_directories(src)
-cc_directories(tests)
 
 if(BUILD_TOOLS)
     cc_directories(tools)
diff --git b/CMakeLists.txt.rej b/CMakeLists.txt.rej
new file mode 100644
index 0000000..159e4b8
--- /dev/null
+++ b/CMakeLists.txt.rej
@@ -0,0 +1,7 @@
+--- CMakeLists.txt
++++ CMakeLists.txt
+@@ -129,4 +194,4 @@ if(BUILD_PYTHON_BINDINGS)
+ 
+     message(STATUS "Zvec install path: ${ZVEC_PY_INSTALL_DIR}")
+     install(TARGETS _zvec LIBRARY DESTINATION ${ZVEC_PY_INSTALL_DIR})
+-endif()+endif()
diff --git a/src/binding/c/CMakeLists.txt b/src/binding/c/CMakeLists.txt
index 3007478..8fc09f7 100644
--- a/src/binding/c/CMakeLists.txt
+++ b/src/binding/c/CMakeLists.txt
@@ -136,7 +136,7 @@ elseif(APPLE)
             Arrow::arrow_acero
             rocksdb
             glog
-            libprotobuf
+            protobuf::libprotobuf
             antlr4
     )
     
diff --git a/src/core/CMakeLists.txt b/src/core/CMakeLists.txt
index 76bebd9..15e72fe 100644
--- a/src/core/CMakeLists.txt
+++ b/src/core/CMakeLists.txt
@@ -42,7 +42,7 @@ endif()
 cc_library(
     NAME zvec_core STATIC STRICT PACKED
     SRCS ${ALL_CORE_SRCS}
-    LIBS zvec_ailego zvec_turbo sparsehash magic_enum rabitqlib
+    LIBS zvec_ailego zvec_turbo sparsehash magic_enum 
     INCS . ${PROJECT_ROOT_DIR}/src/core
     VERSION "${GIT_SRCS_VER}"
 )
diff --git a/src/core/algorithm/hnsw_rabitq/CMakeLists.txt b/src/core/algorithm/hnsw_rabitq/CMakeLists.txt
index ed547dc..aaba4f6 100644
--- a/src/core/algorithm/hnsw_rabitq/CMakeLists.txt
+++ b/src/core/algorithm/hnsw_rabitq/CMakeLists.txt
@@ -15,7 +15,7 @@ cc_library(
     NAME core_knn_hnsw_rabitq
     STATIC SHARED STRICT ALWAYS_LINK
     SRCS *.cc
-    LIBS core_framework rabitqlib sparsehash
+    LIBS core_framework sparsehash
     INCS . ${PROJECT_ROOT_DIR}/src ${PROJECT_ROOT_DIR}/src/core ${PROJECT_ROOT_DIR}/src/core/algorithm
     VERSION "${PROXIMA_ZVEC_VERSION}"
-  )
\ No newline at end of file
+  )
diff --git a/src/core/interface/CMakeLists.txt b/src/core/interface/CMakeLists.txt
index 9e1a2f6..06d6e12 100644
--- a/src/core/interface/CMakeLists.txt
+++ b/src/core/interface/CMakeLists.txt
@@ -5,6 +5,6 @@ cc_library(
         NAME core_interface STATIC STRICT ALWAYS_LINK
         SRCS *.cc indexes/*.cc
         INCS . ${PROJECT_ROOT_DIR}/src/ ${PROJECT_ROOT_DIR}/src/core
-        LIBS zvec_ailego core_framework sparsehash magic_enum rabitqlib
+        LIBS zvec_ailego core_framework sparsehash magic_enum 
         VERSION "${PROXIMA_ZVEC_VERSION}"
 )
diff --git a/src/db/CMakeLists.txt b/src/db/CMakeLists.txt
index b268927..32dd449 100644
--- a/src/db/CMakeLists.txt
+++ b/src/db/CMakeLists.txt
@@ -25,11 +25,11 @@ cc_library(
     roaring
     rocksdb
     antlr4
-    libprotobuf
+    protobuf::libprotobuf
     Arrow::arrow_static
     Arrow::arrow_compute
     Arrow::arrow_dataset
     Arrow::arrow_acero
   DEPS zvec_proto
   VERSION "${PROXIMA_ZVEC_VERSION}"
-)
\ No newline at end of file
+)
diff --git a/src/db/common/concurrent_roaring_bitmap.h b/src/db/common/concurrent_roaring_bitmap.h
index 8d5f106..a0e9764 100644
--- a/src/db/common/concurrent_roaring_bitmap.h
+++ b/src/db/common/concurrent_roaring_bitmap.h
@@ -16,8 +16,8 @@
 #include <mutex>
 #include <shared_mutex>
 #include <sstream>
-#include <roaring.hh>
-#include <roaring64map.hh>
+#include <roaring/roaring.hh>
+#include <roaring/roaring64map.hh>
 #include <roaring/roaring.h>
 #include <zvec/ailego/internal/platform.h>
 #include <zvec/ailego/io/file.h>
diff --git a/src/db/index/segment/segment_helper.cc b/src/db/index/segment/segment_helper.cc
index 2c1038f..7ff91f1 100644
--- a/src/db/index/segment/segment_helper.cc
+++ b/src/db/index/segment/segment_helper.cc
@@ -30,7 +30,7 @@
 #include "db/index/common/index_filter.h"
 #include "db/index/common/meta.h"
 #include "db/index/storage/forward_writer.h"
-#include "roaring.hh"
+#include <roaring/roaring.hh>
 
 namespace zvec {
 
@@ -836,4 +836,4 @@ Status SegmentHelper::ExecuteDropScalarIndexTask(DropScalarIndexTask &task) {
       &task.output_scalar_indexer_);
 }
 
-}  // namespace zvec
\ No newline at end of file
+}  // namespace zvec
