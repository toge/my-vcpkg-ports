libvips provides pkg-config modules:

    vips

In CMake projects, use:

    find_package(PkgConfig REQUIRED)
    pkg_check_modules(VIPS REQUIRED IMPORTED_TARGET vips)
    target_link_libraries(main PRIVATE PkgConfig::VIPS)
